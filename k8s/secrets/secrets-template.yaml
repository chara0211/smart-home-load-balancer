# Template pour les secrets Kubernetes
# IMPORTANT: Ne commitez JAMAIS ce fichier avec des valeurs réelles
# Utilisez kubectl pour créer les secrets

# Exemple de création de secrets :
# kubectl create secret generic postgres-secret \
#   --from-literal=username=smarthome \
#   --from-literal=password=VOTRE_MOT_DE_PASSE \
#   --from-literal=database=smarthome \
#   -n smarthome

# kubectl create secret generic rabbitmq-secret \
#   --from-literal=username=guest \
#   --from-literal=password=VOTRE_MOT_DE_PASSE \
#   -n smarthome

# kubectl create secret generic mongodb-secret \
#   --from-literal=username=admin \
#   --from-literal=password=VOTRE_MOT_DE_PASSE \
#   -n smarthome

# kubectl create secret generic keycloak-secret \
#   --from-literal=username=admin \
#   --from-literal=password=VOTRE_MOT_DE_PASSE \
#   -n smarthome

# kubectl create secret docker-registry registry-secret \
#   --docker-server=your-registry.com \
#   --docker-username=your-username \
#   --docker-password=your-password \
#   --docker-email=your-email@example.com \
#   -n smarthome

