apiVersion: v1
kind: Service
metadata:
  name: keycloak-service
  namespace: smarthome
  labels:
    app: keycloak
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080
    protocol: TCP
    name: http
  selector:
    app: keycloak

