apiVersion: v1
kind: Service
metadata:
  name: cassandra-service
  namespace: smarthome
  labels:
    app: cassandra
spec:
  clusterIP: None
  ports:
  - port: 9042
    name: cql
  - port: 7000
    name: intra-node
  selector:
    app: cassandra

