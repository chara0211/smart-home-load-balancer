apiVersion: v1
kind: Service
metadata:
  name: billing-service-nodeport
  namespace: smarthome
  labels:
    app: billing-service
spec:
  type: NodePort
  ports:
  - port: 8086
    targetPort: 8086
    nodePort: 30086
    protocol: TCP
    name: http
  selector:
    app: billing-service

