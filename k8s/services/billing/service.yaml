apiVersion: v1
kind: Service
metadata:
  name: billing-service
  namespace: smarthome
  labels:
    app: billing-service
spec:
  type: ClusterIP
  ports:
  - port: 8086
    targetPort: 8086
    protocol: TCP
    name: http
  selector:
    app: billing-service

