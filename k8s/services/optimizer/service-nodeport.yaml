apiVersion: v1
kind: Service
metadata:
  name: optimizer-service-nodeport
  namespace: smarthome
  labels:
    app: optimizer-service
spec:
  type: NodePort
  ports:
  - port: 8085
    targetPort: 8085
    nodePort: 30085
    protocol: TCP
    name: http
  selector:
    app: optimizer-service

