apiVersion: v1
kind: Service
metadata:
  name: optimizer-service
  namespace: smarthome
  labels:
    app: optimizer-service
spec:
  type: ClusterIP
  ports:
  - port: 8085
    targetPort: 8085
    protocol: TCP
    name: http
  selector:
    app: optimizer-service

