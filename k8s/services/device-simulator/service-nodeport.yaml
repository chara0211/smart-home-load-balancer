apiVersion: v1
kind: Service
metadata:
  name: device-simulator-service-nodeport
  namespace: smarthome
  labels:
    app: device-simulator-service
spec:
  type: NodePort
  ports:
  - port: 8082
    targetPort: 8082
    nodePort: 30082
    protocol: TCP
    name: http
  selector:
    app: device-simulator-service

