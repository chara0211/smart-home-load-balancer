apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: device-simulator-service-hpa
  namespace: smarthome
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: device-simulator-service
  minReplicas: 1
  maxReplicas: 3
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70

