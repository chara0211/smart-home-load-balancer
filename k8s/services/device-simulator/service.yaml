apiVersion: v1
kind: Service
metadata:
  name: device-simulator-service
  namespace: smarthome
  labels:
    app: device-simulator-service
spec:
  type: ClusterIP
  ports:
  - port: 8082
    targetPort: 8082
    protocol: TCP
    name: http
  selector:
    app: device-simulator-service

